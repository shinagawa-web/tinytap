name: ClusterFuzzLite batch fuzzing

permissions:
  contents: read

on:
  schedule:
    - cron: '0 3 * * *' # Once a day. Short (60s) libFuzzer run via ClusterFuzzLite — not required for
                         # Scorecard's Fuzzing check (that only checks for .clusterfuzzlite/Dockerfile's
                         # presence, not that this workflow ever runs), just a cheap extra exploration pass.

jobs:
  BatchFuzzing:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        sanitizer:
          - address
    steps:
      - name: Build Fuzzers (${{ matrix.sanitizer }})
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@884713a6c30a92e5e8544c39945cd7cb630abcd1 # v1
        with:
          language: go
          sanitizer: ${{ matrix.sanitizer }}
      - name: Run Fuzzers (${{ matrix.sanitizer }})
        id: run
        uses: google/clusterfuzzlite/actions/run_fuzzers@884713a6c30a92e5e8544c39945cd7cb630abcd1 # v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          fuzz-seconds: 60
          mode: 'batch'
          sanitizer: ${{ matrix.sanitizer }}
          output-sarif: true
