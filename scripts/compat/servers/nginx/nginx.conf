worker_processes 1;
events { worker_connections 1024; }

http {
    server {
        listen 8080;

        # TESTDATA_PATH is replaced by start.sh with the absolute testdata/ path.
        root TESTDATA_PATH;

        location / {
            try_files $uri =404;
        }
    }
}
